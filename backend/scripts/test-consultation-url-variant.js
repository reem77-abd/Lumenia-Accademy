/* REMOVED: URL-variant consultation test
   The route POST /api/consultations/course/:courseId/chapter/:chapterId was reverted
   per developer request. Use the canonical endpoint POST /api/consultations/chapter/:chapterId.
*/

console.error('test-consultation-url-variant.js: REMOVED — the URL-variant was deleted.');
process.exitCode = 1;

